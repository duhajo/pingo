<div class="row">
  <div class="large-4 large-centered columns center white-panel" id="login-page">
    <h2>Sign in</h2>
    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
      <div><%= f.label :login %><br />
      <%= f.text_field :login, :autofocus => true %></div>

      <div>
        <div class="row">
          <div class="small-6 columns">
            <%= f.label :password %>
          </div>
          <div class="small-6 columns">
            <%- if devise_mapping.recoverable? && controller_name != 'passwords' %>
              <%= link_to "Forgot your password?", new_password_path(resource_name), :class => "small-link right" %><br />
            <% end -%>
          </div>
        </div>
        <%= f.password_field :password %>
      </div>

      <% if devise_mapping.rememberable? -%>
        <div><%= f.check_box :remember_me %> <%= f.label :remember_me %></div>
      <% end -%>

      <div class="clearfix">
          <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
            <%= link_to "Sign up", new_registration_path(resource_name), :class => "sec-button button left" %>
          <% end -%>
          <%= f.submit "Sign in", :class => "button right" %>
      </div>
    <% end %>
    <!--render "users/shared/links"-->
  </div>
</div>